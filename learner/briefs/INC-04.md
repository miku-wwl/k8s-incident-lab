# 事件简报 — INC-04

## 初始告警

多个用户流程在同一时间窗口内开始超时。

## 已观察到的症状

- 多个服务出现超时或错误。
- 大多数应用 Pods 处于 Running。
- 整个应用集群的 CPU 和内存都没有明显饱和。

## 背景

服务在退化开始前处于健康状态，具有代表性的流量仍在持续运行。

## 响应者任务

不要假设每个服务都是独立失败；找出共同故障域，缓解级联影响，并验证所有受影响的用户流程。
