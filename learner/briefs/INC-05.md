# 事件简报 — INC-05

## 初始告警

客户对 API 的请求出现间歇性 503 或超时。

## 已观察到的症状

- Deployment 报告 Available。
- Pods 处于 Running，重启次数为零。
- CPU 和内存看起来正常。
- 同一客户路径同时存在成功和失败请求。

## 背景

在仪表盘的第一层视图中，工作负载本身看起来是绿色健康状态。

## 响应者任务

追踪完整请求路径，找出可用流量在哪个环节中断，安全缓解问题，并从客户边界验证恢复。
